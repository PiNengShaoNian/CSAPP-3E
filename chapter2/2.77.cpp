int main() {
    // K = 17 = (2 ^ 4 + 1)
    // x * 17 = (x << 4) + x

    // K = -7 = (1 - 2 ^ 3)
    // x * -7 = (x - (x << 3))

    // K = 60 = 2 ^ 6 - 2 ^ 2
    // x * 60 = (x << 6) - (x << 2)

    // K = -112 = 2 ^ 4 - 2 ^ 7
    // x * -112 = (x << 4) - (x << 7)
    return 0;
}