这道题要求你分析一个程序中潜在的加载-存储相互影响

A. 相当于a[i] = a[i + 1]，对于0 <= i <= 998,他要将每个元素a[i]设置为i + 1
B. 相当于a[i + 1] = a[i], 对于1 <= i <= 999,他将每个元素a[i]设置为0
C. 对于第二种情况，每次迭代的加载都依赖于前一次迭代的结果。因此，在连续
   的迭代之间有写/读相关。
D. 得到的CPE等于1.2，与示例A的相同，这是应为存储和后续的加载没有相关。
