我们可以看到，这个函数在连续的迭代之间有读/写相关----一次迭代的目的之p[i]与
下一次的源值p[i-1]相同。因此，每次迭代形成的关键路径就包含: 一次存储
(来自前一次迭代),一次加载和浮点加。当存在数据相关时，测量到的CPE值为
9.0，与write_read的CPE测量值7.3是一致的，因为write_read包括一个整数加
(1时钟周期的延迟)，而psum1包括一个浮点加(3时钟周期延迟)。