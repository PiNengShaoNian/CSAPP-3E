多项式求值时解决许多问题的核心技术。例如，多项式函数常常用作对数学库
中三角函数求近似值。
A. 这个函数执行2n个乘法和n个加法
B. 我们可以看到，这里限制性能的计算是反复的计算表达式result = a[i] + x * result
   从来自上一次迭代的result的值开始，我们必须先把他乘x(5个时钟周期),然后
   机上a[i](3个时钟周期)，然后我们得出本次迭代的值。因此，每次迭代造成了
   最小延迟8个周期，正好等于我们测量到的CPE